# Database Configuration
# Note: Using asyncpg driver for async support
DATABASE_URL=postgresql+asyncpg://postgres:123qwe@localhost:5432/atbnet
DATABASE_SCHEMA=idiorag
DATABASE_POOL_SIZE=20
DATABASE_MAX_OVERFLOW=10

# Environment-specific database URLs (uncomment as needed)
# DATABASE_URL_TESTING=postgresql+asyncpg://postgres:123qwe@localhost:5432/atbnet_test
# DATABASE_URL_PRODUCTION=postgresql+asyncpg://user:pass@prod-host:5432/atbnet

# LLM Configuration
# Qwen3-14B endpoint
LLM_API_URL=https://yyc-130-250-171-122.cloud.denvrdata.com/v1
LLM_MODEL_NAME=your-model-name-here(e.g., Qwen/Qwen3-14B)
LLM_API_KEY=your-llm-token-here
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=2048

# Model-specific stop sequences to prevent rambling (optional)
# These are tuned for Qwen3. Adjust for other models (GPT, Claude, etc.)
# Leave empty or omit to disable: LLM_STOP_SEQUENCES=[]
LLM_STOP_SEQUENCES=["\n\nOkay,", "\n\nLet me", "\n\nWait,", "\n\nHowever,"]

# JWT Configuration
# HS256 (symmetric) - shared secret with your application
JWT_SECRET_KEY=your-application-secret-key-here
JWT_ALGORITHM=HS256
# Note: RS256 is more secure for production. Consider migrating later.
# JWT_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----...

# Application Settings
APP_NAME=IdioRAG
APP_VERSION=0.1.0
ENVIRONMENT=development
LOG_LEVEL=INFO
CORS_ORIGINS=["http://localhost:3000"]

# Vector Store Configuration
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
EMBEDDING_DIMENSION=384
CHUNK_SIZE=512
CHUNK_OVERLAP=50

# API Configuration
API_V1_PREFIX=/api/v1
MAX_UPLOAD_SIZE_MB=10
